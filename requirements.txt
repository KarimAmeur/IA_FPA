--extra-index-url https://download.pytorch.org/whl/cpu
--no-cache-dir
pip==25.1.1  # Force la version la plus récente
# CORE (obligatoire)
streamlit==1.32.0
numpy==1.24.0
pandas==2.0.0

# NLP (version light)
spacy==3.7.2 ; python_version < '3.11'
sentence-transformers==2.2.2

# PyTorch (CPU only)
torch==2.0.0
torchvision==0.15.1